# PIKIT v4.8 Balance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Checklist:** 항상 `/Users/moomi/Downloads/z03.Vibe_Coding/PIKIT/checklist.md` 를 먼저 확인하고, 완료 시 업데이트할 것.

**Goal:** PIKIT v4.8 — HE 52~53% 달성 (2~100명), 어댑티브 시스템 곡괭이, 동적 잭팟 풀 구현

**Architecture:** constants.js 파라미터 조정 → GameEngine.js 로직 추가 → 시뮬레이터로 검증 → 에픽 스펙 전체 업데이트

**Tech Stack:** Node.js, server/game/constants.js, server/game/GameEngine.js, tools/balance-v48-sim.js

**Spec:** `docs/superpowers/specs/2026-03-23-balance-v48-design.md`

---

## Task 1: constants.js 업데이트

**Files:**
- Modify: `server/game/constants.js`

- [ ] **Step 1: 시스템 곡괭이 약화 모드 상수 추가**

`system` 타입에 weak 모드 스탯 추가:

```js
system: {
  // ... 기존 유지 (활성 모드)
  name: 'PIKIT',
  price: 0,
  damage: 5,
  scale: 1.5,
  gravityMult: 0.3,
  speedMult: 0.1,
  lifetime: Infinity,  // 앵커는 Infinity 유지
  texture: 'system_pickaxe.png',
  color: '#FF00FF',
  description: 'PIKIT house pickaxe — competes with players for blocks',
},
system_weak: {
  name: 'PIKIT',
  price: 0,
  damage: 5,
  scale: 0.8,          // 작게
  gravityMult: 0.5,    // 덜 느리게
  speedMult: 0.05,     // 더 느리게
  lifetime: 60000,     // 60초 (dynamic pickaxes)
  texture: 'system_pickaxe.png',
  color: '#888888',    // 회색
  description: 'PIKIT (Resting) — reduced activity in low-player mode',
},
```

- [ ] **Step 2: 곡괭이 가격 6% 인하**

```js
basic:  { price: 3200, ... }   // 3400 → 3200
power:  { price: 8300, ... }   // 8800 → 8300
light:  { price: 3700, ... }   // 3900 → 3700
swift:  { price: 3400, ... }   // 3600 → 3400
```

- [ ] **Step 3: JACKPOT_POOL_CONFIG 추가 (JACKPOT_CONFIG 아래)**

```js
const JACKPOT_POOL_CONFIG = {
  HOUSE_PROFIT_MILESTONE: 50000,  // 하우스 수익 5만마다
  POOL_ALLOCATION: 2500,          // 잭팟 풀에 2,500 추가
  MIN_POOL_TO_SPAWN: 50000,       // 최소 5만 이상일 때만 스폰
};
```

- [ ] **Step 4: JACKPOT_CONFIG 업데이트**

```js
const JACKPOT_CONFIG = {
  SPAWN_THRESHOLD: 0,           // 더 이상 소비 기반 아님 (풀 기반으로 대체)
  SPAWN_CHANCE: 0.0001,         // 0.01% (기존 0.05% → 극악화)
  RESPAWN_CHANCE: 0.00001,      // 이탈 후 재스폰 확률 0.001%
  REWARD: 0,                    // 고정 보상 폐지 (동적 풀로 대체)
  HP: 1500,                     // 300 → 1500 (레이드 요소)
  MIN_PLAYERS: 0,               // 플레이어 수 조건 제거 (풀 기반으로 충분)
};
```

- [ ] **Step 5: module.exports에 JACKPOT_POOL_CONFIG 추가**

```js
module.exports = {
  GAME,
  PICKAXE_TYPES,
  TNT_TYPES,
  BLOCK_TYPES,
  JACKPOT_CONFIG,
  JACKPOT_POOL_CONFIG,   // 추가
  COMBO,
  HOUSE_EDGE,
  INITIAL_BALANCE,
};
```

- [ ] **Step 6: 커밋**
```bash
git add server/game/constants.js
git commit -m "feat(balance): v4.8 constants - adaptive sys pickaxe, price -6%, dynamic jackpot pool config"
```

---

## Task 2: GameEngine.js — 어댑티브 시스템 곡괭이 로직

**Files:**
- Modify: `server/game/GameEngine.js`

- [ ] **Step 1: import에 JACKPOT_POOL_CONFIG 추가**

```js
const { GAME, PICKAXE_TYPES, TNT_TYPES, COMBO, BLOCK_TYPES, JACKPOT_CONFIG, JACKPOT_POOL_CONFIG } = require('./constants');
```

- [ ] **Step 2: 상단 상수 수정**

```js
const SYSTEM_PICKAXE_TARGET = 1;   // 기존 유지 (앵커용)
const DYNAMIC_SYSTEM_RATIO_THRESHOLDS = [
  { minPicks: 81,  sysCnt: 4 },
  { minPicks: 51,  sysCnt: 3 },
  { minPicks: 21,  sysCnt: 2 },
  { minPicks: 4,   sysCnt: 1 },
  { minPicks: 0,   sysCnt: 0 },   // 솔로(≤3): 앵커만 (약화 모드)
];
const WEAK_MODE_THRESHOLD = 3;     // 플레이어 곡괭이 ≤ 3 → 앵커 약화 모드
const MAX_SYSTEM_PICKAXES = 4;
```

- [ ] **Step 3: constructor에 새 상태 변수 추가**

```js
// 기존 _systemPickaxeCount 유지
this._anchorPickaxeId = null;      // 앵커 ID 추적
this._dynamicSysCount = 0;         // 60초짜리 동적 sys 수

// 잭팟 풀
this.jackpotPool = 0;
this.houseProfitAccumulator = 0;
```

- [ ] **Step 4: start() 수정 — 앵커 1개 스폰 (Infinity)**

```js
start() {
  // ... 기존 코드 유지 ...

  // 앵커 1개 스폰 (Infinity lifetime)
  this._systemPickaxeCount = 0;
  this._spawnAnchorPickaxe();
}
```

- [ ] **Step 5: `_spawnAnchorPickaxe()` 메서드 추가**

```js
_spawnAnchorPickaxe() {
  const pickaxe = new Pickaxe('system', '__system__', 'PIKIT');
  pickaxe.y = this.cameraY - GAME.INTERNAL_HEIGHT / 2;
  pickaxe.x = this._findEmptySpawnX(pickaxe.y);
  this.pickaxes.set(pickaxe.id, pickaxe);
  this._anchorPickaxeId = pickaxe.id;
  this._systemPickaxeCount++;
}
```

- [ ] **Step 6: `_spawnDynamicSystemPickaxe(mode)` 메서드 추가**

```js
_spawnDynamicSystemPickaxe(mode = 'system') {
  // mode: 'system' (full) or 'system_weak'
  const pickaxe = new Pickaxe(mode, '__system__', 'PIKIT');
  pickaxe.y = this.cameraY - GAME.INTERNAL_HEIGHT / 2;
  pickaxe.x = this._findEmptySpawnX(pickaxe.y);
  this.pickaxes.set(pickaxe.id, pickaxe);
  this._systemPickaxeCount++;
  this._dynamicSysCount++;
}
```

- [ ] **Step 7: tick() 내 시스템 곡괭이 관리 로직 교체**

기존 단순 카운터 체크를 아래로 교체:

```js
// tick() 내 시스템 곡괭이 섹션
if (now - this.lastSystemPickaxe > this.systemPickaxeInterval) {
  const playerPickaxeCount = this.pickaxes.size - this._systemPickaxeCount;

  // 앵커 복구 (만약 사라진 경우 - 정상적으론 안 사라지지만 방어 로직)
  if (!this.pickaxes.has(this._anchorPickaxeId)) {
    this._spawnAnchorPickaxe();
    this.lastSystemPickaxe = now;
    return; // 이번 틱은 앵커 복구만
  }

  // 앵커 모드 업데이트 (약화/활성)
  const anchorPickaxe = this.pickaxes.get(this._anchorPickaxeId);
  if (anchorPickaxe) {
    const shouldBeWeak = playerPickaxeCount <= WEAK_MODE_THRESHOLD;
    if (shouldBeWeak && anchorPickaxe.type !== 'system_weak') {
      anchorPickaxe.color = '#888888';
      anchorPickaxe.speedMult = 0.05;
      anchorPickaxe.scale = 0.8;
      anchorPickaxe.type = 'system_weak';
    } else if (!shouldBeWeak && anchorPickaxe.type !== 'system') {
      anchorPickaxe.color = '#FF00FF';
      anchorPickaxe.speedMult = 0.1;
      anchorPickaxe.scale = 1.5;
      anchorPickaxe.type = 'system';
    }
  }

  // 동적 시스템 곡괭이 목표 계산
  let dynamicTarget = 0;
  for (const t of DYNAMIC_SYSTEM_RATIO_THRESHOLDS) {
    if (playerPickaxeCount >= t.minPicks) {
      dynamicTarget = t.sysCnt;
      break;
    }
  }
  dynamicTarget = Math.min(dynamicTarget, MAX_SYSTEM_PICKAXES - 1); // 앵커 제외

  if (this._dynamicSysCount < dynamicTarget) {
    this._spawnDynamicSystemPickaxe('system');
    this.lastSystemPickaxe = now;
  }
}
```

- [ ] **Step 8: `_expirePickaxe()` 에서 카운터 분기 수정**

```js
_expirePickaxe(pickaxe) {
  if (pickaxe.ownerId === '__system__') {
    this._systemPickaxeCount--;
    if (pickaxe.id !== this._anchorPickaxeId) {
      this._dynamicSysCount = Math.max(0, this._dynamicSysCount - 1);
    }
    return; // 시스템 곡괭이는 보상 없음
  }

  // 플레이어 곡괭이: 하우스 수익 계산 및 잭팟 풀 적립
  const profit = pickaxe.price - pickaxe.totalReward;
  if (profit > 0) {
    this.houseProfitAccumulator += profit;
    // 5만 마일스톤마다 잭팟 풀에 2,500 할당
    while (this.houseProfitAccumulator >= JACKPOT_POOL_CONFIG.HOUSE_PROFIT_MILESTONE) {
      this.jackpotPool += JACKPOT_POOL_CONFIG.POOL_ALLOCATION;
      this.houseProfitAccumulator -= JACKPOT_POOL_CONFIG.HOUSE_PROFIT_MILESTONE;
    }
  }

  // 기존 플레이어 알림 로직 유지
  const player = this.players.get(pickaxe.ownerId);
  if (player) {
    player.activePickaxes = player.activePickaxes.filter(id => id !== pickaxe.id);
    const socket = this.io.sockets.sockets.get(pickaxe.ownerId);
    if (socket) {
      socket.emit('pickaxeExpired', {
        pickaxeId: pickaxe.id,
        type: pickaxe.type,
        totalReward: pickaxe.totalReward,
        blocksDestroyed: pickaxe.blocksDestroyed,
      });
    }
  }
}
```

- [ ] **Step 9: 잭팟 스폰 조건 업데이트 (Chunk.js 또는 GameEngine.js 내)**

Chunk.js의 잭팟 블록 스폰 조건을 풀 기반으로 변경:
```js
// 잭팟 스폰 eligible 조건:
// 1. jackpotPool >= JACKPOT_POOL_CONFIG.MIN_POOL_TO_SPAWN (5만 이상)
// 2. !jackpotBlockExists
// 3. SPAWN_CHANCE 통과
```

GameEngine에서 Chunk 생성 시 jackpotPool 정보 전달하도록 수정:
```js
getOrCreateChunk(index) {
  if (!this.chunks.has(index)) {
    this.chunks.set(index, new Chunk(index, this)); // engine 전달 (기존 유지)
  }
  return this.chunks.get(index);
}
// Chunk.js에서 this.engine.jackpotPool 접근 가능
```

- [ ] **Step 10: 잭팟 파괴 보상 동적으로 변경**

`_checkPickaxeCollisions` 내 잭팟 블록 파괴 처리 수정:
```js
if (block.type === 'jackpot') {
  this.jackpotBlockExists = false;
  const jackpotReward = this.jackpotPool;
  this.jackpotPool = 0;   // 풀 리셋

  player.earn(jackpotReward, block.name);
  // ... 기존 알림 로직, reward 변수를 jackpotReward로 교체
}
```

- [ ] **Step 11: 잭팟 이탈 시 풀 유지 + 재스폰 확률 조정**

`_clearBlocksAboveViewport` 내 잭팟 이탈 처리:
```js
if (block.type === 'jackpot') {
  this.jackpotBlockExists = false;
  // jackpotPool 리셋 없음 (유지)
  // RESPAWN_CHANCE로 재스폰 가능하게 (Chunk.js에서 처리)
}
```

- [ ] **Step 12: broadcast()에 jackpotPool 정보 추가**

```js
const state = {
  // ... 기존 필드들 ...
  jackpotPool: this.jackpotPool,   // 클라이언트에 풀 규모 표시용
};
```

- [ ] **Step 13: 커밋**
```bash
git add server/game/GameEngine.js
git commit -m "feat(balance): v4.8 adaptive system pickaxe (max 4, weak mode) + dynamic jackpot pool"
```

---

## Task 3: Chunk.js 잭팟 스폰 조건 업데이트

**Files:**
- Modify: `server/game/Chunk.js`

- [ ] **Step 1: Chunk.js 읽기 및 현재 잭팟 스폰 로직 파악**

- [ ] **Step 2: 잭팟 스폰 조건 변경**

기존: `creditsSinceLastJackpot >= SPAWN_THRESHOLD && playerCount >= MIN_PLAYERS`
신규: `engine.jackpotPool >= JACKPOT_POOL_CONFIG.MIN_POOL_TO_SPAWN && !engine.jackpotBlockExists`

- [ ] **Step 3: 잭팟 블록 HP 업데이트**

잭팟 블록이 spawn될 때 `JACKPOT_CONFIG.HP` (1500) 사용하도록 수정

- [ ] **Step 4: 이탈 후 재스폰 로직**

이탈된 잭팟(풀 유지 상태)에서 다음 청크 생성 시:
```js
const respawnChance = JACKPOT_CONFIG.RESPAWN_CHANCE; // 0.001%
```

- [ ] **Step 5: 커밋**
```bash
git add server/game/Chunk.js
git commit -m "feat(balance): v4.8 jackpot spawn - pool-based condition, HP 1500, respawn logic"
```

---

## Task 4: 신규 시뮬레이터 작성

**Files:**
- Create: `tools/balance-v48-sim.js`

- [ ] **Step 1: 시뮬레이터 기본 구조 작성**

v4.7 시뮬레이터(balance-v47-final.js) 기반으로 아래 변경 반영:
- 새 가격 (basic=3200, power=8300, light=3700, swift=3400)
- 어댑티브 sys 곡괭이 (WEAK 모드 steal율 별도 계산)
- 플레이어 범위: [1, 2, 5, 10, 15, 20, 30, 50, 80, 100]
- 각 플레이어 수에서 sys 목표 자동 결정

```js
function getDynamicSysCount(playerPickaxes) {
  if (playerPickaxes <= 3)  return { cnt: 1, mode: 'weak' };
  if (playerPickaxes <= 20) return { cnt: 1, mode: 'full' };
  if (playerPickaxes <= 50) return { cnt: 2, mode: 'full' };
  if (playerPickaxes <= 80) return { cnt: 3, mode: 'full' };
  return { cnt: 4, mode: 'full' };
}
```

- [ ] **Step 2: 어댑티브 steal율 계산**

```js
const SYS_RATE_FULL = 0.656;   // enc(system full)
const SYS_RATE_WEAK = 0.185;   // enc(system weak: scale=0.8, speed=0.05, grav=0.5)
const AVG_PLAYER_RATE = 3.0;

function calcSteal(playerPicks, sysCnt, sysMode) {
  const sysRate = sysMode === 'weak' ? SYS_RATE_WEAK : SYS_RATE_FULL;
  const totalSysRate = sysCnt * sysRate;
  return totalSysRate / (totalSysRate + playerPicks * AVG_PLAYER_RATE);
}
```

- [ ] **Step 3: 잭팟 풀 시뮬레이션 추가**

```js
function simJackpotPool(totalSpent, heRate) {
  const houseProfit = totalSpent * heRate;
  const milestones = Math.floor(houseProfit / 50000);
  return milestones * 2500;
}
```

- [ ] **Step 4: 플레이어별 HE 출력**

```js
for (const pc of [1, 2, 5, 10, 15, 20, 30, 50, 80, 100]) {
  const playerPicks = pc * 1.5;
  const { cnt, mode } = getDynamicSysCount(playerPicks);
  const steal = calcSteal(playerPicks, cnt, mode);
  // ... HE 계산 및 출력
  console.log(`P${pc.toString().padStart(3)} | sys=${cnt}(${mode}) | steal=${(steal*100).toFixed(2)}% | HE=${(he*100).toFixed(1)}%`);
}
```

- [ ] **Step 5: 분포 검증 (per-pickaxe percentile)**

basic 곡괭이 기준 회수율 분포 출력:
- P5, P10, P25, P50, P75, P90, P95 (price 대비 %)

- [ ] **Step 6: 커밋**
```bash
git add tools/balance-v48-sim.js
git commit -m "feat(tools): balance-v48 simulator with adaptive sys, 1-100 players, per-pickaxe distribution"
```

---

## Task 5: 시뮬레이터 실행 및 밸런스 검증

**Files:** (실행만, 파일 변경 없음)

- [ ] **Step 1: 시뮬레이터 실행**
```bash
node tools/balance-v48-sim.js
```

- [ ] **Step 2: 검증 기준 확인**

| 기준 | 목표 | 합격? |
|------|------|-------|
| HE @ 2~30명 | 52~53% | |
| HE @ 30~100명 | 52~53% | |
| HE 솔로 | ≤55% | |
| P10 회수율 (basic) | ≥8% of price | |
| P50 회수율 (basic) | 25~50% of price | |
| P90+ 회수율 | ≥100% (profit) | |

- [ ] **Step 3: 기준 미달 시 조정**

기준 미달 시 constants.js 파라미터 조정 후 재실행:
- HE 높으면: 가격 추가 인하 or 블록 보상 소폭 인상
- HE 낮으면: 가격 소폭 인상
- 분포 불균형: 블록 가중치 조정

- [ ] **Step 4: 검증 통과 후 커밋**
```bash
git add server/game/constants.js  # 조정된 경우만
git commit -m "fix(balance): v4.8 simulation-verified params - HE 52-53% confirmed"
```

---

## Task 6: Epic 스펙 전체 업데이트

**Files:**
- Modify: `docs/superpowers/specs/epic-05-곡괭이구매-채굴.md`
- Modify: `docs/superpowers/specs/epic-09-잭팟시스템.md`
- Modify: `docs/superpowers/specs/epic-15-시스템곡괭이.md`
- Create/Modify: 기타 epic 파일들 (누락 epic 확인 및 추가)

- [ ] **Step 1: epic-05 가격 업데이트**
  - Basic 3,400→3,200, Power 8,800→8,300, Light 3,900→3,700, Swift 3,600→3,400
  - 스토리 내 가격 수치 전부 교체 (Hardcore 10배도 함께)

- [ ] **Step 2: epic-09 잭팟 시스템 전면 개편**
  - 고정 250,000cr 보상 → 동적 풀 보상
  - 등장 조건: 1.5M 소비 → 잭팟 풀 ≥ 50,000cr
  - HP: 300 → 1,500 (레이드 요소 추가)
  - 이탈 시 처리: 카운터 리셋 → 풀 유지, HP 회복, 재스폰 극악 확률
  - Story 9-8 업데이트: "소멸 시 풀 유지" 반영
  - Story 9-9 업데이트: "풀 기반 조건" 반영
  - Story 추가: 잭팟 풀 표시 (UI에 현재 풀 금액 노출)

- [ ] **Step 3: epic-15 시스템 곡괭이 업데이트**
  - 어댑티브 모드 (휴식/활성) 추가
  - 최대 4개 (동적 조절) 반영
  - 솔로 모드 시각적 표현 스토리 추가
  - steal율 테이블 업데이트 (100명까지)

- [ ] **Step 4: 누락 epic 확인 및 추가**
  - epic-16-하우스수익추적.md (잭팟 풀 적립 메커니즘)
  - 기능명세체크리스트.md 업데이트

- [ ] **Step 5: 커밋**
```bash
git add docs/superpowers/specs/
git commit -m "docs: v4.8 epic specs - updated prices, adaptive sys pickaxe, dynamic jackpot pool"
```

---

## Task 7: 코드 리뷰

- [ ] **Step 1: superpowers:requesting-code-review 스킬 호출하여 변경 사항 전체 리뷰**
- [ ] **Step 2: 리뷰 피드백 반영**
- [ ] **Step 3: checklist.md 최종 업데이트**

---

## 실행 순서 요약

```
Task 1 (constants) → Task 2 (GameEngine) → Task 3 (Chunk)
  → Task 4 (Simulator 작성) → Task 5 (검증 + 튜닝)
  → Task 6 (Epic 스펙 업데이트) → Task 7 (코드 리뷰)
```
